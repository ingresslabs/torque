apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "torque-guardian-incident-e2e.fullname" . }}
  labels:
    {{- include "torque-guardian-incident-e2e.labels" . | nindent 4 }}
data:
  mode: {{ .Values.config.mode | quote }}
  password: {{ .Values.config.password | quote }}
