apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "torque-applyplan-e2e.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "torque-applyplan-e2e.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "torque-applyplan-e2e.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "torque-applyplan-e2e.fullname" . }}
      containers:
        - name: pause
          image: registry.k8s.io/pause:3.10
          env:
            - name: TORQUE_E2E_TEXT
              value: {{ .Values.config.text | quote }}
            - name: TORQUE_E2E_ENABLED
              value: {{ ternary "true" "false" .Values.config.enabled | quote }}

