apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "verify-findings.fullname" . }}
  labels:
    app: {{ include "verify-findings.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "verify-findings.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "verify-findings.name" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
