apiVersion: torque.dev/v1
kind: Stack
name: e2e-07-inheritance
defaults:
  cluster: { name: archimedes, kubeconfig: ~/.kube/archimedes.yaml }
  namespace: torque-stack-e2e
  tags: [root]
profiles:
  dev:
    defaults:
      tags: [dev]

