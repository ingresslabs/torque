# Complex hooks stack fixture

This stack fixture is used by the integration test `TestTorqueStackComplexHooksE2E`.

The `stack.yaml` contains placeholder tokens (e.g. `__TORQUE_TEST_NAMESPACE__`) that
the test replaces at runtime after copying this directory into a temp root.
